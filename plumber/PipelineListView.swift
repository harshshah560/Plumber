import SwiftUI



struct WorkflowListView: View {
    var body: some View {
        // Using an EmptyView ensures this file compiles instantly
        // and resolves the "unable to type-check" error.
        EmptyView()
    }
}
